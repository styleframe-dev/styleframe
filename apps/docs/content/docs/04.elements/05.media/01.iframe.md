---
title: Iframe Element
description: Remove the default border from iframe elements using the useIframeElement composable.
navigation:
    title: Iframe
    icon: false
---

::note{icon="i-lucide-package"}
**Part of the Global Preset:** `useIframeElement` is included in the [Global Preset](/docs/theme/elements/preset) (`useGlobalPreset`) and you can configure it through the preset's `iframe` option. For most projects, applying it via the preset is the recommended approach.
::

## Overview

The `useIframeElement` composable removes the default border from `iframe` elements. This is a config-free composable — it applies a fixed style with no customization options.

::story-preview
---
story: theme-elements-iframe--default
panel: true
---
::

## `useIframeElement`

```ts
function useIframeElement(s: Styleframe): void
```

::tabs
:::tabs-item{icon="i-lucide-code" label="Code"}

```ts [styleframe.config.ts]
import { styleframe } from 'styleframe';
import { useIframeElement } from '@styleframe/theme';

const s = styleframe();

useIframeElement(s);

export default s;
```

:::
:::tabs-item{icon="i-lucide-file-input" label="Output"}

```css [styleframe/index.css]
iframe {
    border: 0;
}
```

:::
::

This composable takes no configuration options, creates no CSS custom properties, and returns `void`.
