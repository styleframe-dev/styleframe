---
"styleframe": patch
"@styleframe/theme": patch
---

docs: update recipe documentation to use destructuring pattern

Updated recipe examples to use destructured `recipe` function and demonstrate both `ref()` and `@variable.name` syntax for referencing design tokens.
