import { createUseRecipe } from "../../utils/createUseRecipe";

/**
 * Toast content recipe for the main text area.
 */
export const useToastContentRecipe = createUseRecipe("toast-content", {
	base: {
		flex: "1 1 auto",
	},
});
