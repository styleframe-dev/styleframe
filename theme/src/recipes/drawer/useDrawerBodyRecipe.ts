import { createOverlayBodyRecipe } from "../modal/createOverlayRecipes";

/**
 * Drawer body recipe for the main content area.
 */
export const useDrawerBodyRecipe = createOverlayBodyRecipe("drawer");
