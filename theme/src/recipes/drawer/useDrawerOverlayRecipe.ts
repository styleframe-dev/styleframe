import { createBackdropRecipe } from "../modal/createOverlayRecipes";

/**
 * Drawer overlay recipe for the backdrop behind the drawer.
 */
export const useDrawerOverlayRecipe = createBackdropRecipe("drawer");
