import { createOverlayFooterRecipe } from "./createOverlayRecipes";

/**
 * Modal footer recipe with top separator and right-aligned actions.
 */
export const useModalFooterRecipe = createOverlayFooterRecipe("modal");
